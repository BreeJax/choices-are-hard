 babel src/app.js --out-file=public/scripts/app.js --presets=env,react


--This code gets babel to basically act like gulp.


if you add --watch at the end, that takes over terminal so that it can watch the changes that you make activly, and make it's own changes accordingly.


live-server public
