console.log("this is sparta.js is running")

const square = (x) => x * x

const add = (a, b) => a + b

//name export
export const minus = (a, b) => a - b

//named exports
export { square, add }

// exports - default -named exports
